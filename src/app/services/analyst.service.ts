import { Injectable } from '@angular/core';

@Injectable({ providedIn: 'root' })
export class AnalystService {
  constructor() {}
  // Métodos futuros: verSolicitudesAsignadas, actualizarEstado, etc.
}
